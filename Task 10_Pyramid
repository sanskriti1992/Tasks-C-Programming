#include<stdio.h>
#include<conio.h>
void main()
{
clrscr();
int rows;
printf("Enter number of rows:");
scanf("%d",&rows);
for(int i=rows;i>=1;i--)
{
for(int j=1;j<=1;j++)
{
printf("%d",j);
}
printf("\n");
getch();
}
